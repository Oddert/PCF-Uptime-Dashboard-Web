/**
 * Local Storage access key for the Access Token.
 * @constant
 * @category Constants
 * @subcategory App Constants
 */
export const PCF_DASHBOARD_ACCESS_TOKEN_LS_KEY =
    'PCF_DASHBOARD_ACCESS_TOKEN_LS_KEY';

/**
 * Local Storage access key for the Refresh Token.
 * @constant
 * @category Constants
 * @subcategory App Constants
 */
export const PCF_DASHBOARD_REFRESH_TOKEN_LS_KEY =
    'PCF_DASHBOARD_REFRESH_TOKEN_LS_KEY';

/**
 * Width of the main drawer panel when in open state.
 * @constant
 * @category Constants
 * @subcategory App Constants
 */
export const headerDrawerOpenWidth = 280;
